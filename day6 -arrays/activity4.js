// Task 10: Use a for loop to iterate over the array and log each element to the console.

const arr = [1,2,3,4,5,6];
const array = [1,2,3,4,5,6];

// for(let i=0; i<arr.length; i++) {
    // console.log(arr[i]);
// }


// Task 11: Use the forEach method to iterate over the array and log each element to the console

array.forEach(element => {
    console.log(element);
});

/* 
Use for loop when you need more control over the iteration, access to the index, or need to use break or continue.

Use forEach when you want a simpler and more readable way to perform an operation on each element of an array without needing to exit early or skip iterations. 
*/